[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
dynamic = ["version"]
name = "bc_fsf_pages"
dependencies = [
]
requires-python = ">= 3.8"
description = "Add static pages to a site"
readme = "README.md"

[tool.setuptools]
package-dir = {"" = "src"}